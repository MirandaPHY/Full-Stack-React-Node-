/*
3. Use map para transformar um array de números, multiplicando cada número por 5
*/  

const numeros = [1, 2, 3, 4, 5];
const numerosVezesCinco = numeros.map((numero) => numero * 5);
console.log(numerosVezesCinco); // [5, 10, 15, 20, 25]